-# 商品出品完了画面
.sale-done
  = render partial: 'sub_header'
  .sale-done__header
    %h2.done-title
      出品が完了しました！
    .done-message
      %p あなたが出品した商品は
      = link_to "「出品した商品一覧」", user_path(current_user), class:"btn"
      %p からいつでも見ることが出来ます。
    .done-contents
      .continue-sale
        = link_to "続けて出品する",new_product_path, class:"btn"
      .return-top
        =link_to 'TOPページへ戻る',root_path, class:"return_btn"
  = render partial: 'sub_footer'